import Rect from './rect';
import Matrix from './matrix';
import Point from './point';

export default class Path {
    public static initRect(rect: Rect, transform: Matrix) {
        const path = new Path();
    }

    public postions: Point[] = [];
}
